# Copy to your shell environment (or a .env you source). Never commit real keys.

# Required — your OpenAI API key.
OPENAI_API_KEY=sk-your-key-here

# Optional — where the Flask service binds (defaults shown).
HOST=0.0.0.0
PORT=8000
