[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Real errors only (syntax + pyflakes) — no style noise.
select = ["E9", "F"]
